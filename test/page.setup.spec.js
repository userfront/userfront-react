const { Test } = require("./config/test.utils.js");
import { toolkit } from "../src/index.js";

xdescribe("Class based rendering", () => {
  it("should render the signup form properly", () => {
    expect(false).toEqual(true);
  });
});
